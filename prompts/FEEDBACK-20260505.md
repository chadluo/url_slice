# Feedback

## Interactions

- [x] new subdomains add to the left -- new segments prepend to the left (`example.com` becomes `a.example.com` then
  `b.a.example.com`), and add subdomain button always on left end

## Interfaces

- [x] add subdomain button use `+` for label and browser native style
