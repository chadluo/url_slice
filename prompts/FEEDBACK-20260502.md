# Feedback

## UI Implementations

- [ ] currently the UI is migrated from Tailwind and contains a lot of inline styles. Extract them to separate CSS
  files, add proper ids and classNames for elements.
- [ ] use a vertical flex with `1lh` gap for url-slice-app layout, remove all hrs, don't worry separator border for now

## Interfaces

- [ ] url-slice-app padding to `0.75lh 0.5em`
- [ ] for each editor, make the symbol (`//` for host, `:` for port etc) same element as the title and don't apply
  special styling.
- [ ] also for each editor, make the title not selectable by cursor.
- [ ] apply button use `AccentColor` and `AccentColorText` for background and text
