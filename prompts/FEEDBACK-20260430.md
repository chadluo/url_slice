# Feedback

## Logic

- [ ] Bug: ticking/unticking search param items enable/disable random items, they don't match -- this may not be
  accurate, could be clicking and item reordering race condition, don't action on it first
- [x] Bug: fragments tab doesn't switch, currently it locks to text fragments

## Interactions

- [ ] domain host should be readonly, don't put it in a text input
- [ ] move '+sub' button to the left of domain host field
- [ ] don't reorder enabled/disabled search params when selecting them; only reorder when the popup/sidepanel is
  reloaded

## Interfaces

- [ ] UI font too small, remove 75% on body, 0.85em on URLs and 0.8em on field labels -- they all can use 1rem
- [ ] side panel mode don't need width/height on app component, remove them
- [ ] '+Add param' and '+Add text fragment' buttons should use lighter color on hover -- use oklch color functions to
  calculate the corresponding color
