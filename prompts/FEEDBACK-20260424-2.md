# Feedback

## Logic

- [x] query params add a checkbox (default checked) before each pair, so that the value is appended only when the box is
  checked. unchecked pairs are not sent but saved, so that next time user opens the popup the values are still
  available.

## Interactions

- [x] make query param close button easier to access -- make it a button with hover styles

## Interfaces

- [ ] popup.html follow current page color scheme and automatically show light or dark mode
- [x] instead of `font-[monospace]`, update the Tailwind css base font setting to use browser default monospace for
  `--font-mono`, also revert existing `font-[monospace]` styles
- [x] only use monospace for URL components: preview, domain, port, path etc.; titles, buttons and other UI labels just
  use default sans font. eventually popup.html should have a default font setting and components won't need to override
