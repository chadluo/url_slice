# Feedback

## Logic

- [x] when editing search params don't append empty pairs (url shouldn't include `?=` or `?=&=` segments), use
  `URLSearchParams` API
- [ ] for chrome urls (schema `chrome://`) override the base domain rule, so that for example on `chrome://extensions` I
  can click on `extensions` and select other chrome URLs like `downloads`, `settings` etc.

## Interactions

- [x] port add two extra buttons -1000 and +1000
- [x] fragments always show the text/media tabs -- currently on first run it shows the `Add text fragment` and `Add
  media fragment` options but changes to tabs when the user chooses any of them. This is confusing and the UI should be
  stable.
- [x] text fragments also use the checkbox for turning on or turning off but saved, same as search params

## Interfaces

- [x] make popup window wider, aim at 600px
