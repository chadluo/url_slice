# Feedback

## Logic & Interactions

- [x] subdomains: similar to paths, subdomain segments shouldn't contain common parts. For example
  `url.spec.whatwg.org`, when clicking on `url`, options should render as `html` instead of `html.spec` for the domain
  `html.spec.whatwg.org`
- [x] if one path segment is updated, clear up all following up segments
- [ ] path segment dropdowns should also include a text input field to manually type desired segment.

## Interfaces

- [ ] the base domain should be rendered as plain text not a pill, since it's not clickable
- [ ] for ports, paths and fragments, use explicit titles similar to query params instead of only symbols
- [ ] decode percent-encoding paths
- [ ] use userstyles monospace font (css `monospace`)
